package contacts;

// add, remove, edit, count, info, exit
// person, organization
// name, surname, birth, gender, number
public enum Action {
    ADD,
    REMOVE,
    EDIT,
    COUNT,
    INFO,
    EXIT,
    PERSON,
    ORGANIZATION,
    NAME,
    SURNAME,
    BIRTH,
    GENDER,
    NUMBER;
}