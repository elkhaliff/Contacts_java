package contacts;

// add, list, search, count, exit
    // back, again
        // edit, delete, menu
// person, organization
    // name, surname, birth, gender, number
    // name, address, number
public enum Action {
    ADD,
    LIST,
    SEARCH,
    BACK,
    AGAIN,
    EDIT,
    DELETE,
    MENU,
    COUNT,
    EXIT,
    PERSON,
    ORGANIZATION,
    NAME,
    SURNAME,
    BIRTH,
    GENDER,
    ADDRESS,
    NUMBER
}